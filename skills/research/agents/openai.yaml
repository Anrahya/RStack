interface:
  display_name: "Research"
  short_description: "Research a question, compare options or check a current"
  default_prompt: "Use $research for the requested help only. Do not start or resume R-Stack mode."
policy:
  allow_implicit_invocation: true

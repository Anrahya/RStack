interface:
  display_name: "Review"
  short_description: "Try to falsify a change without inventing issues"
  default_prompt: "Use $review to review this change skeptically."
policy:
  allow_implicit_invocation: true

interface:
  display_name: "Comment review"
  short_description: "Review comments for redundancy, staleness and useful rationale"
  default_prompt: "Use $no-comments for the requested help only. Do not start or resume R-Stack mode."
policy:
  allow_implicit_invocation: true

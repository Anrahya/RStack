interface:
  display_name: "Test-first development"
  short_description: "Use a failing-test-first loop for an explicitly requested"
  default_prompt: "Use $tdd for the requested help only. Do not start or resume R-Stack mode."
policy:
  allow_implicit_invocation: true

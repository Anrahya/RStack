interface:
  display_name: "Blast radius"
  short_description: "Assess what a proposed code, API, configuration or schema"
  default_prompt: "Use $blast-radius for the requested help only. Do not start or resume R-Stack mode."
policy:
  allow_implicit_invocation: true

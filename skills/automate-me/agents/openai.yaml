interface:
  display_name: "Automate me"
  short_description: "Draft reusable preferences or a personal skill from explicit"
  default_prompt: "Use $automate-me for the requested help only. Do not start or resume R-Stack mode."
policy:
  allow_implicit_invocation: true

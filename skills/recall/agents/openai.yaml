interface:
  display_name: "Recall"
  short_description: "Reconstruct recent context on a named topic from available"
  default_prompt: "Use $recall for the requested help only. Do not start or resume R-Stack mode."
policy:
  allow_implicit_invocation: true

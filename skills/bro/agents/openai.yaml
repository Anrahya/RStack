interface:
  display_name: "Bro"
  short_description: "Restate the last answer in plain language"
  default_prompt: "Use $bro for the requested help only. Do not start or resume R-Stack mode."
policy:
  allow_implicit_invocation: true

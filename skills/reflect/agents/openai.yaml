interface:
  display_name: "Reflect"
  short_description: "Turn repeated work patterns into targeted improvements"
  default_prompt: "Use $reflect to extract decision-changing lessons from this work."
policy:
  allow_implicit_invocation: true

interface:
  display_name: "Reflect"
  short_description: "Review a learning, research or engineering session for"
  default_prompt: "Use $reflect for the requested help only. Do not start or resume R-Stack mode."
policy:
  allow_implicit_invocation: true

interface:
  display_name: "Orchestrate"
  short_description: "Coordinate bounded subagent work and integrate evidence"
  default_prompt: "Use $orchestrate to decompose and coordinate this task."
policy:
  allow_implicit_invocation: true

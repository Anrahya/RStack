# Merge into existing project instructions, do not overwrite them

When an AGENTS.md exists alongside CLAUDE.md, import it with @AGENTS.md.
Use the installed RStack r-stack-mode skill by default for non-trivial repository
engineering. The r-stack command is an alias of the same workflow. Read applicable
nested instructions before editing. Standalone utility calls run only the utility;
resume paused engineering only when separately requested.
